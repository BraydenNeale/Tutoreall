<%# if conversation.is_unread?(current_user) %>
	<%#= link_to 'mark as read', mark_as_read_conversation_path(conversation), method: :post %>
<%# end %>

<!-- <li> -->
	<% if conversation.is_unread?(current_user) %>
		<%= link_to participant_name(conversation), conversation_path(conversation) %> - new <br>
	<% else %>
		<%= link_to participant_name(conversation), conversation_path(conversation) %> <br>
	<% end %>
<!-- </li> -->